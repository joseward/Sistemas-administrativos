const fs = require('fs');
const path = require('path');

const file = path.join(__dirname, 'src/app/horarios/page.tsx');
// I will just use `git checkout` to restore the file, and then carefully apply my change.
